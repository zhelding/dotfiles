#!/bin/sh

STATUS=$(pymodoro -o -ah)
PREFIX=${STATUS:0:1}

if [ "$PREFIX" = "P" ]; then
    echo %{T2}🍅%{T-} ${STATUS: -5}
elif [ "${STATUS: -3}" != "min" ] && [ "${STATUS: -1}" != "h" ] \
    && [ ! -z "$STATUS" ]; then
    echo %{T2}☕%{T-} ${STATUS: -5}
else
    echo
fi
